# SHRAVAN Mobile Client

Flutter client for the SHRAVAN health platform.

Run from this directory:

```bash
flutter pub get
flutter run
```

Configure the backend endpoint in the mobile client before running against a deployed API.
